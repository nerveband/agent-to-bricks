# Quick Check Prompt

Copy-paste this into Claude Code to verify everything builds without releasing:

---

Run all CI checks locally: version sync, CLI tests, GUI typecheck, plugin lint, website build. Report what passes and what fails.
